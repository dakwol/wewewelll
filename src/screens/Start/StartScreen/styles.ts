import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  item: {
    borderWidth: 1,
    borderStyle: 'solid',
    margin: 10,
    width: 150,
    alignItems: 'center',
  },
});

export default styles;
